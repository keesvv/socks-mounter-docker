# socks-mounter-docker

Mount a remote filesystem through SOCKS5, in Docker.

## Usage

See [docker-compose.yml](docker-compose.yml) for an example.

## Filesystem support

Currently only SSHFS is supported.

## Author

Kees van Voorthuizen
